; A command-driven LED worker that signals completion with PIO IRQ flag 0.
; Bit 0 is the output level; bits 1..31 are a delay-loop count.
.pio_version 0
.program irq_led

.wrap_target
    pull block
    out pins, 1
    out x, 31
    irq 0
delay:
    jmp x-- delay
.wrap
